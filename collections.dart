// List

main() {
  List names_and_ages = ['israk', 'riya', '25', '24'];

  for (var n in names_and_ages) {
    //print(n);
  }

  List<String> last_names = ['ahmed', 'jahan'];

  print(last_names.indexOf('jahan'));

  for (var n in last_names) {
    //print(n);
  }
}
